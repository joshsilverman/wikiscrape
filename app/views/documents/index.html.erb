<h1>Listing Docs</h1>

<table>
  <tr>
    <th>Doc Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @documents.each do |doc| %>
  <tr>
    <td><%= doc.name %></td>
    <td><%= link_to 'Show', doc %></td>
    <td><%= link_to 'Edit', edit_document_path(doc) %></td>
    <td><%= link_to 'Destroy', doc, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New List', new_document_path %>
